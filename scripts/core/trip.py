
class Trip(object):
	def __init__(self, origin, destination)
	self.origin
	self.

# geocode itself

trip.distance, trip.duration, trip.cost, trip.origin

departure_time, distance_value,	duration_value,	duration_in_traffic_value, 
end_lat, end_lon, fare_value, mode, origin, pair, place_id, rank, start_lat,
start_lon, status
